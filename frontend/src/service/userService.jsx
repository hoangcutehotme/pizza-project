const loginAPI = (email, password) => {
    // return axios.post(`${url}/api/auth/login`, { email, password });
    return (email === "huynhbathuan" && password === "huynhbathuan");
}

export {loginAPI} 
  